box: ruby:2.2.4

dev:
    steps:
        - bundle-install
        - internal/watch:
            code: bundle exec rspec
            reload: true

build:
    steps:
        - bundle-install
        - script:
            name: rspec
            code: bundle exec rspec

        # TODO:
        #    - run contracts
        #    - deploy to Heroku
